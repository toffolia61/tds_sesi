let produto1 = 100;
let produto2 = 10;
let valortotal = produto1 + produto2
const mensagem1 = valortotal < 100 ? "o valor do carrinho é " + valortotal : "o valor do carrinho com a pexincha é " + (valortotal - (valortotal * 0.10 ))
console.log(mensagem1)